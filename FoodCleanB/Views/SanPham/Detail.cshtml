@model FoodCleanB.Database.SanPham

@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/LayoutShare.cshtml";
}

<div class="nav">
    <a href="@Url.Action("List","SanPham", new { Page = 1, Id = Model.NhomHang.MaSo, Title = Model.NhomHang.Ten.ToLower().Replace(" ","-")})">@Model.NhomHang.Ten</a> / @Model.TenHang
</div>

<!-- Start Shop Detail  -->
<div class="shop-detail-box-main">
    <div class="container">
        <div class="row">
            <div class="col-xl-5 col-lg-5 col-md-6">
                <div id="carousel-example-1" class="single-product-slider carousel slide" data-ride="carousel" >
                    <div class="carousel-inner" role="listbox">
                        <div class="carousel-item active"> <img class="d-block w-100 img-responsive img-thumbnail" src="@Model.AnhSanPham" alt="First slide"> </div>
                        
                    </div>
                </div>
            </div>
            <div class="col-xl-7 col-lg-7 col-md-6">
                <div class="single-product-details">
                    <h2>@Model.TenHang</h2>
                    <h5>
                        @if (Model.KhuyenMai != null)
                        {
                            <del>@Model.GiaThanh đ</del>
                            @Html.Raw((Model.GiaThanh - Model.KhuyenMai) + "đ")
                        }
                        else
                        {
                            @Html.Raw(Model.GiaThanh)
                        }

                    </h5>
                    <h4>Kho: @Model.SoLuong</h4>
                    <h4>Mô tả sản phẩm</h4>
                    <p>
                        @Html.Raw(Model.MoTa)
                    </p>

                    <div class="price-box-bar add-to-btn">
                        <div class="cart-and-bay-btn mb-2">
                            <a class="btn hvr-hover add-card" data-itemid="@Model.MaHang" href="#">
                                <i class="fas fa-cart-plus"></i> Thêm vào giỏ
                            </a>
                            <a class="btn hvr-hover" href="#" data-itemid="@Model.MaHang">
                                <i class="fas fa-heart"></i> Add to wishlist
                            </a>
                        </div>
                        <div class="share-bar">
                            <a class="btn hvr-hover" href="#"><i class="fab fa-facebook" aria-hidden="true"></i></a>
                            <a class="btn hvr-hover" href="#"><i class="fab fa-google-plus" aria-hidden="true"></i></a>
                            <a class="btn hvr-hover" href="#"><i class="fab fa-twitter" aria-hidden="true"></i></a>
                            <a class="btn hvr-hover" href="#"><i class="fab fa-pinterest-p" aria-hidden="true"></i></a>
                            <a class="btn hvr-hover" href="#"><i class="fab fa-whatsapp" aria-hidden="true"></i></a>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="col-12">
                <h4>Chi tiết:</h4>
                <p>
                    @Html.Raw(Model.MoTa)
                </p>
            </div>
        </div>

     
    </div>
</div>
<!-- End Cart -->
